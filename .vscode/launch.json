{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/openGL.exe",
            "args": [],
            "cwd": "${workspaceFolder}/bin",
            "preLaunchTask": "build"
        }
    ]
}